services:
  mysql:
    image: mysql:8.0
    container_name: upvane-laravel-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: upvane
      MYSQL_USER: upvane
      MYSQL_PASSWORD: upvane
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uupvane", "-pupvane"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - mysqldata:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: upvane-laravel-phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: mysql
      PMA_USER: upvane
      PMA_PASSWORD: upvane
    ports:
      - "8081:80"
    depends_on:
      mysql:
        condition: service_healthy

volumes:
  mysqldata:
